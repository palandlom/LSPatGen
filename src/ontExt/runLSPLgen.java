package ontExt;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * @author lomov
 *
 */
public class runLSPLgen
{

	private static final Logger LOGGER = LoggerFactory
			.getLogger(runLSPLgen.class);

	public static void main(String[] args)
	{
		/*
		 * String[] synL = { "клиент1","клиент1" }; Concept lCon = new
		 * Concept("клиент", synL);
		 * 
		 * String[] synR = { "сервер1","сервер1" }; Concept rCon = new
		 * Concept("сервер", synR);
		 * 
		 * String[] synRel = { "оказываться","выполняться" }; Relation rel = new
		 * Relation("отношение", synRel);
		 * 
		 * Branch branch = new Branch(lCon, rel, rCon);
		 * 
		 * LSPLGenerator gen = new LSPLGenerator("Obj"); List<String> rules =
		 * gen.getLSPLrules(branch);
		 * 
		 * for (String rule : rules) { System.out.println(rule); }
		 * 
		 */

	}

}
